<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BMSBahrain.Views.AboutPage"
             xmlns:vm="clr-namespace:BMSBahrain.ViewModels"
             xmlns:resources="clr-namespace:BMSBahrain.Resx"
             >
    
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    <ContentPage.ToolbarItems>
        <ToolbarItem  Text="{Static resources:AppResources.UnderConstruction}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#7585a9</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid Padding="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Text="{Static resources:AppResources.UnderConstruction}"/>
    </Grid>

</ContentPage>
